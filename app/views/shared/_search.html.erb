<div class="form-inline">
    <%= form_tag(listevol_path, method: 'get', role: 'form') do %>
  		<div class="form-group"> 
        	<%= text_field_tag :search, params[:search], class: 'form-control', placeholder: "Saisir ville arrivée" %>
        </div>

        <div class="form-group" style="height: 50px;"> 
        	<%= submit_tag "Search", :titre => nil, class: "btn btn-primary" %>
        </div>
    <% end %>
</div>
