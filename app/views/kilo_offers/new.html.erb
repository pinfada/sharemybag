<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1><%= t('kilo_offers.new.title') %></h1>
      <p class="lead"><%= t('kilo_offers.new.subtitle') %></p>
      <%= render 'form' %>
    </div>
  </div>
</div>
